name: kachaka_bgm_while_running
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  grpc: ^3.2.4
  protobuf: ^3.1.0
  async: ^2.11.0
  provider: ^6.1.2
  audioplayers: ^5.2.0
  fixnum: ^1.1.0
  audioplayers_linux: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
